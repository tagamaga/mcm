@model IEnumerable<MCM.Models.PostalCode>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.PostalCodeActive)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.PostalCodeActive)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PostalCodeID }) |
            @Html.ActionLink("Details", "Details", new { id=item.PostalCodeID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PostalCodeID })
        </td>
    </tr>
}

</table>
